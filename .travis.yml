sudo: required
language: scala
cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt/boot/
    - geoip-resources
jdk:
  - oraclejdk8

services:
  - docker

script:
  - sbt test docker:publishLocal

after_success:
  - [ "$TRAVIS_BRANCH" == "master" ] && ./publish-image.sh
